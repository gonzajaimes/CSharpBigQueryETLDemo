using Google.Cloud.BigQuery.V2;
using System;

class TestConnection
{
  /*  static void Main(string[] args)
   {
     try
     {
       // Replace this with your actual Project ID
       string projectId = "car-wash-etl";

       var client = BigQueryClient.Create(projectId);

       Console.WriteLine("✅ Connection to BigQuery succeeded!");
       Console.WriteLine("Available datasets in project:");

       foreach (var dataset in client.ListDatasets(projectId))
       {
         Console.WriteLine($"- {dataset.Reference.DatasetId}");
       }
     }
     catch (Exception ex)
     {
       Console.WriteLine("❌ Failed to connect to BigQuery:");
       Console.WriteLine(ex.Message);
     }
   }*/
}
